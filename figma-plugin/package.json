{
  "name": "figma-to-html-css",
  "version": "1.0.0",
  "description": "Figma plugin that converts designs to production-ready HTML/CSS",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "typescript": "^5.4.0",
    "esbuild": "^0.21.0"
  },
  "license": "MIT"
}
